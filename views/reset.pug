extends layout

block content
  .inner
    form.form(method="POST")
      h2 Нова парола
      label(for="password") Парола
      input(type="password" name="password")
      label(for="password-confirm") Потвърди паролата
      input(type="password" name="password-confirm")
      input(type="submit" value="Изпрати →")
